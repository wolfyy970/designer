/**
 * Default local dev ports when `PORT` / `VITE_PORT` are unset.
 * Keep in sync with `scripts/kill-dev-servers.sh` and `package.json` `dev:all` wait-on fallback.
 */
export const DEFAULT_DEV_API_PORT = 4731;
export const DEFAULT_DEV_CLIENT_PORT = 4732;

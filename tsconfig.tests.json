{
  "//": "Type-checks the test files. `tsconfig.app.json` excludes them, so before this config existed no test was type-checked and fixtures could silently drift away from the production types (stale `modelNodeIds`, casts hiding wrong field shapes, typo'd import paths).",
  "extends": "./tsconfig.app.json",
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.tests.tsbuildinfo",
    "types": ["vite/client", "node"]
  },
  "include": ["src"],
  "exclude": []
}

/** Static copy for Canvas Manager (SpecManager) — keeps UI strings in one place. */

export const CANVAS_MANAGER_LOAD_FAILED =
  'Could not load that entry. It may be missing, corrupted, or out of date.';

export const CANVAS_MANAGER_RELOAD_CONFIRM =
  'Replace the open document with the last saved copy from this list? Unsaved changes in the spec sections will be lost, and the graph will reset.';

/** Local-storage caution — single line in Canvas Manager. */
export const CANVAS_MANAGER_STORAGE_WARNING =
  'WARNING: Saved canvases stay in this browser only (local storage).';

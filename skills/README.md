# Agent skills (repo-backed)

Skill packages for future Pi / Agent Skills integration live here as plain files (e.g. `skills/<key>/SKILL.md`).

The agentic sandbox **does not** mount this tree yet. `_manifest.json` may record metadata if skills were migrated from an old database export.
